1. super-admin audit is not viewable
2. super-admin > guild manage as administrator is not functioning properly
3. migration tracker: accepted applications aren't viewable anywhere while a window is open (queue only shows applied/waitlisted), and there's no way to undo an accidental "Accept" click through the UI — the app disappears from the queue with no reachable row to reverse the decision from